String ternary() {
  print('============ternary===============');

  String name = "shivam";

  String returnValue = name.startsWith("s")
      ? "Yes valuse starts with s"
      : "valsue does not starts with s";

  return returnValue;
}
